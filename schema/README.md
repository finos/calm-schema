# schema

The current, in-development CALM Meta Schema — edited in place, with no version subfolders.

Released versions are available as git tags (e.g. `v1.2`) and as npm releases of
[`@finos/calm-schema`](https://www.npmjs.com/package/@finos/calm-schema), not as folders here.
